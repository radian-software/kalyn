(public data (List a)
  Null (Cons a (List a)))

(public data Char (Char Int))

(public def main (IO Empty)
  (let ((str "Hello, world!\n"))
    (case str
      (Null
       (print "oh noes\n"))
      (rst
       (print "yay\n")))))
